# OPC-Starter 开发环境
# 使用 MSW Mock 模式，无需真实 Supabase 即可运行
# 启动: docker compose -f docker-compose.dev.yml up
services:
  app:
    build:
      context: .
      target: dev
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    env_file:
      - .env.test
