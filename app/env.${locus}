# 环境变量模板文件
# 
# 使用说明：
# 1. 复制此文件为 .env.local 或 .env.test
# 2. 根据实际情况修改配置值
# 3. 不要提交 .env.* 文件到 Git（已在 .gitignore 中）
#
# 启动模式：
# - npm run dev       → 使用 .env.local（连接真实 Supabase 后端）
# - npm run dev:test  → 使用 .env.test（使用 MSW Mock 数据）

# MSW Mock 开关
# true: 启用 MSW Mock，false: 关闭 MSW，直接连接后端
VITE_ENABLE_MSW=false

# Supabase 配置
# 请替换为你的真实 Supabase 项目信息
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Supabase Edge Function URL（视频生成功能需要）
# 格式: https://YOUR_PROJECT_REF.supabase.co/functions/v1
VITE_SUPABASE_EDGE_FUNCTION_URL=your_supabase_edge_function_url

# API 基础路径
VITE_API_BASE_URL=/api

# Mock 数据配置（仅在 MSW 模式下生效）
VITE_MOCK_DATA_ENABLED=true