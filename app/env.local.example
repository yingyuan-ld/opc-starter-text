# OPC-Starter - 真实 Supabase 模式环境变量模板
# 复制此文件为 .env.local 并填写实际值

# ============================================
# Supabase 配置（必需）
# ============================================
# 从 Supabase Dashboard → Settings → API 获取
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# ============================================
# 阿里云 OSS 配置（前端，可选）
# ============================================
# 注意: OSS 上传使用 STS 临时凭证，无需配置 AccessKey
# 以下配置仅用于传输加速

# OSS 传输加速（可选，启用后访问更快）
VITE_OSS_ACCELERATE_ENABLED=false
VITE_OSS_ACCELERATE_DOMAIN=your-bucket.oss-accelerate.aliyuncs.com

# ============================================
# 开发环境配置
# ============================================

# 启用 MSW Mock（开发调试用）
VITE_ENABLE_MSW=false

# 日志级别
VITE_LOG_LEVEL=debug

# ============================================
# Edge Functions 环境变量（参考）
# ============================================
# ⚠️ 以下变量需要在 Supabase Dashboard → Edge Functions → Secrets 配置
# ⚠️ 不要在前端代码中使用这些变量，此处仅作参考
#
# ALIYUN_BAILIAN_API_KEY=sk-xxx     # 百炼 AI API Key
# DASHSCOPE_API_KEY=sk-xxx          # DashScope API Key
# OSS_ACCESS_KEY_ID=xxx             # RAM 用户 AccessKey
# OSS_ACCESS_KEY_SECRET=xxx         # RAM 用户 Secret
# OSS_REGION=oss-cn-hangzhou        # OSS 区域
# OSS_BUCKET=photo-wall-xxx         # OSS Bucket
# OSS_ROLE_ARN=arn:acs:ram::xxx     # STS 角色 ARN
